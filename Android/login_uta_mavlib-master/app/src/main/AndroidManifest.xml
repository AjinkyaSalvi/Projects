<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="edu.uta.mavs.login_uta_mavlib">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_uta"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_uta_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".RegisterUser"></activity>
        <activity android:name=".AboutUs" />
        <activity android:name=".ManageBooks" />
        <activity android:name=".CheckinBooks" />
        <activity android:name=".CheckoutBooks" />
        <activity android:name=".SearchBooks" />
        <activity android:name=".LibrarianMenu" />
        <activity android:name=".StudentMainMenuActivity" />
        <activity android:name=".ui.login.SplashScreenActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.login.LoginActivity"
            android:label="@string/app_name" />
    </application>

</manifest>